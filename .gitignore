# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/.vinext/
/out/

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# local environment
.env*

# deployment and generated files
.vercel
next-env.d.ts
/dist/
/.wrangler/
/outputs/
/work/

# local compiler build tree; the browser runtime is published from /public/runtime
/compiler-experiment/
tsconfig.tsbuildinfo

# The split browser compiler image under /public/runtime is tracked so
# Cloudflare Git and direct-upload deployments are self-contained.
